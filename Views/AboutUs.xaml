<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SmartGolfGlove_V2.Views.AboutUs">
    <VerticalStackLayout>
        <CollectionView x:Name="cliCollection"
                        ItemsLayout="VerticalList"
                        SelectionMode="Single">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Background="#1C375C" Padding="5" Margin="5">
                        <Label Text="{Binding _title}" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Text="Check"
                Clicked="CheckCallBack"/>
    </VerticalStackLayout>
</ContentPage>